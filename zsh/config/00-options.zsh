# History Configuration
setopt HIST_IGNORE_DUPS
setopt HIST_FIND_NO_DUPS
setopt INC_APPEND_HISTORY
setopt SHARE_HISTORY
HISTFILE=~/.zsh/zsh_history
HISTSIZE=10000
SAVEHIST=10000

# zsh compdump
ZSH_COMPDUMP="${HOME}/.zsh/.zcompdump-${HOST}-${ZSH_VERSION}"

